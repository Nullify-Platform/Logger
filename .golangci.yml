run:
  timeout: 8m
  skip-dirs:
    - data
linters:
  disable-all: true
  enable:
    # default
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    # style
    - gofmt
    - stylecheck
    - whitespace
